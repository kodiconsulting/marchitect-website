export default function ServicesHero() {
  return (
    <section className="py-24" style={{ backgroundColor: 'var(--m-bg)' }}>
      <div className="mx-auto max-w-4xl px-6">
        <h1
          className="mb-4 text-6xl font-extrabold leading-tight tracking-tight md:text-7xl"
          style={{ color: 'var(--m-text)' }}
        >
          Services
        </h1>
        <p
          className="mb-6 text-xl font-medium"
          style={{ color: 'var(--m-accent)' }}
        >
          Optional execution to implement your Marchitect roadmap.
        </p>
        <p
          className="mb-4 text-lg leading-relaxed"
          style={{ color: 'var(--m-text-secondary)' }}
        >
          The Marketing Framework Implementation is the core engagement. It does not include full agency execution. Once the framework is installed, execution can be handled by your internal team, your existing vendors, or Marchitect&apos;s execution team as a separate scope.
        </p>
        <p
          className="text-lg leading-relaxed"
          style={{ color: 'var(--m-text-secondary)' }}
        >
          This page covers what Marchitect can execute if you want us in the room.
        </p>
      </div>
    </section>
  )
}
